@page "/Characters"
@page "/Characters/{StatusFilter}/{Letter}/{SpeciesFilter}/{GenderFilter}"
@rendermode InteractiveServer
@inject HttpClient Http
@inject NavigationManager Navigation
@using System.Text.Json.Serialization

<div class="hero-header">
    <h1 class="hero-title">Rick and Morty Characters </h1>
    <p class="hero-subtitle">@filterMessage</p>
</div>

<div class="search-container">
    <input type="text" @bind="searchText" @onkeypress="HandleKeyPress" placeholder="Search characters..." class="search-input" />
    <button @onclick="ApplyFilter" class="search-btn">Search</button>
</div>

<div class="filter-container">
    <div class="filter-section">
        <h3 class="filter-title">Status</h3>
        <div class="status-filters">
            <a href="@BuildUrl("All", Letter, SpeciesFilter, GenderFilter)" class="filter-btn @((string.IsNullOrEmpty(StatusFilter) || StatusFilter == "All") ? "active" : "")">All</a>
            <a href="@BuildUrl("Alive", Letter, SpeciesFilter, GenderFilter)" class="filter-btn alive @(StatusFilter == "Alive" ? "active" : "")">Alive</a>
            <a href="@BuildUrl("Dead", Letter, SpeciesFilter, GenderFilter)" class="filter-btn dead @(StatusFilter == "Dead" ? "active" : "")">Dead</a>
            <a href="@BuildUrl("unknown", Letter, SpeciesFilter, GenderFilter)" class="filter-btn unknown @(StatusFilter == "unknown" ? "active" : "")">Unknown</a>
        </div>
    </div>
    
    <div class="filter-section">
        <h3 class="filter-title">Letters</h3>
        <div class="letter-filters">
            @foreach (var letter in "ABCDEFGHIJKLMNOPQRSTUVWXYZ")
            {
                <a href="@BuildUrl(StatusFilter, letter.ToString(), SpeciesFilter, GenderFilter)" class="letter-btn @(Letter == letter.ToString() ? "active" : "")">@letter</a>
            }
            @if (!string.IsNullOrEmpty(Letter))
            {
                <a href="@BuildUrl(StatusFilter, null, SpeciesFilter, GenderFilter)" class="filter-btn clear">Clear Letter</a>
            }
        </div>
    </div>
    
    <div class="filter-section">
        <h3 class="filter-title">Species</h3>
        <div class="species-filters">
            @foreach (var species in availableSpecies)
            {
                <a href="@BuildUrl(StatusFilter, Letter, species, GenderFilter)" class="filter-btn species @(SpeciesFilter == species ? "active" : "")">@species</a>
            }
            @if (!string.IsNullOrEmpty(SpeciesFilter))
            {
                <a href="@BuildUrl(StatusFilter, Letter, null, GenderFilter)" class="filter-btn clear">Clear Species</a>
            }
        </div>
    </div>
    
    <div class="filter-section">
        <h3 class="filter-title">Gender</h3>
        <div class="gender-filters">
            @foreach (var gender in availableGenders)
            {
                <a href="@BuildUrl(StatusFilter, Letter, SpeciesFilter, gender)" class="filter-btn gender @(GenderFilter == gender ? "active" : "")">@gender</a>
            }
            @if (!string.IsNullOrEmpty(GenderFilter))
            {
                <a href="@BuildUrl(StatusFilter, Letter, SpeciesFilter, null)" class="filter-btn clear">Clear Gender</a>
            }
        </div>
    </div>
    
   
</div>

@if (pagedCharacters == null || !pagedCharacters.Any())
{
    <div class="no-results">
        <p>No characters found</p>
    </div>
}
else
{
    <div class="characters-grid">
        @foreach (var c in pagedCharacters)
        {
            <div class="character-card">
                <div class="character-image">
                    <img src="@c.Image" alt="@c.Name" />
                    <div class="status-badge @c.Status.ToLower()">@c.Status</div>
                </div>
                <div class="character-info">
                    <NavLink href="@($"/CharacterDetails/{c.Id}")" class="character-name">@c.Name</NavLink>
                    <p class="episode-count">@c.Episode.Count episodes</p>
                </div>
            </div>
        }
    </div>
    
    <div class="pagination">
        <button @onclick="PreviousPage" disabled="@(currentPage == 1)" class="page-btn">Previous</button>
        <span class="page-info">Page @currentPage of @totalPages</span>
        <button @onclick="NextPage" disabled="@(currentPage == totalPages)" class="page-btn">Next</button>
    </div>
}

<style>
    .hero-header {
        text-align: center;
        padding: 20px;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 15px;
        margin-bottom: 15px;
        box-shadow: 0 10px 40px rgba(102, 126, 234, 0.3);
    }

    .hero-title {
        font-size: 2.5rem;
        font-weight: 800;
        color: #fff;
        margin: 0;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }

    .hero-subtitle {
        color: #e0e7ff;
        font-size: 1rem;
        margin: 8px 0 0 0;
    }

    .filter-container {
        background: #fff;
        padding: 15px;
        border-radius: 15px;
        box-shadow: 0 5px 20px rgba(0,0,0,0.1);
        margin-bottom: 20px;
    }

    .filter-section {
        margin-bottom: 15px;
        padding-bottom: 15px;
        border-bottom: 1px solid #e0e7ff;
    }

    .filter-section:last-child {
        border-bottom: none;
    }

    .filter-title {
        font-size: 0.95rem;
        font-weight: 700;
        color: #667eea;
        margin: 0 0 10px 0;
        text-align: center;
    }

    .status-filters {
        display: flex;
        gap: 15px;
        justify-content: center;
        flex-wrap: wrap;
    }

    .filter-btn {
        padding: 8px 20px;
        border-radius: 20px;
        text-decoration: none;
        font-weight: 600;
        font-size: 0.9rem;
        transition: all 0.3s;
        border: 2px solid #e0e7ff;
        color: #667eea;
    }

    .filter-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
    }

    .filter-btn.active {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: #fff;
        border-color: #667eea;
    }

    .filter-btn.alive.active { background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%); border-color: #11998e; }
    .filter-btn.dead.active { background: linear-gradient(135deg, #eb3349 0%, #f45c43 100%); border-color: #eb3349; }
    .filter-btn.unknown.active { background: linear-gradient(135deg, #757f9a 0%, #d7dde8 100%); border-color: #757f9a; }

    .letter-filters {
        display: flex;
        gap: 8px;
        justify-content: center;
        flex-wrap: wrap;
    }

    .letter-btn {
        width: 32px;
        height: 32px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 600;
        font-size: 0.85rem;
        transition: all 0.3s;
        border: 2px solid #e0e7ff;
        color: #667eea;
    }

    .letter-btn:hover {
        background: #f3f4f6;
        transform: scale(1.1);
    }

    .letter-btn.active {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: #fff;
        border-color: #667eea;
    }

    .species-filters {
        display: flex;
        gap: 10px;
        justify-content: center;
        flex-wrap: wrap;
    }

    .filter-btn.species.active {
        background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
        border-color: #f093fb;
    }

    .filter-btn.clear {
        background: #f3f4f6;
        color: #6b7280;
        border-color: #d1d5db;
    }

    .filter-btn.clear:hover {
        background: #e5e7eb;
    }

    .gender-filters {
        display: flex;
        gap: 10px;
        justify-content: center;
        flex-wrap: wrap;
    }

    .filter-btn.gender.active {
        background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
        border-color: #fa709a;
    }

    .location-filters {
        display: flex;
        gap: 6px;
        justify-content: center;
        flex-wrap: wrap;
    }

    .location-btn {
        padding: 6px 12px;
        border-radius: 15px;
        text-decoration: none;
        font-weight: 600;
        font-size: 0.75rem;
        transition: all 0.3s;
        border: 2px solid #e0e7ff;
        color: #667eea;
    }

    .location-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(48, 207, 208, 0.3);
    }

    .location-btn.active {
        background: linear-gradient(135deg, #30cfd0 0%, #330867 100%);
        color: #fff;
        border-color: #30cfd0;
    }

    .characters-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
        gap: 25px;
    }

    .character-card {
        background: #fff;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0 5px 20px rgba(0,0,0,0.1);
        transition: all 0.3s;
    }

    .character-card:hover {
        transform: translateY(-10px);
        box-shadow: 0 15px 40px rgba(102, 126, 234, 0.3);
    }

    .character-image {
        position: relative;
        overflow: hidden;
    }

    .character-image img {
        width: 100%;
        height: 200px;
        object-fit: cover;
        transition: transform 0.3s;
    }

    .character-card:hover .character-image img {
        transform: scale(1.1);
    }

    .status-badge {
        position: absolute;
        top: 10px;
        right: 10px;
        padding: 5px 12px;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: 600;
        color: #fff;
        text-transform: uppercase;
    }

    .status-badge.alive { background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%); }
    .status-badge.dead { background: linear-gradient(135deg, #eb3349 0%, #f45c43 100%); }
    .status-badge.unknown { background: linear-gradient(135deg, #757f9a 0%, #d7dde8 100%); }

    .character-info {
        padding: 15px;
        text-align: center;
    }

    .character-name {
        font-size: 1.1rem;
        font-weight: 700;
        color: #1f2937;
        text-decoration: none;
        display: block;
        margin-bottom: 5px;
    }

    .character-name:hover {
        color: #667eea;
    }

    .episode-count {
        color: #6b7280;
        font-size: 0.9rem;
        margin: 0;
    }

    .no-results {
        text-align: center;
        padding: 60px 20px;
        font-size: 1.5rem;
        color: #6b7280;
    }

    .search-container {
        margin-bottom: 20px;
        display: flex;
        gap: 10px;
    }

    .search-input {
        flex: 1;
        padding: 15px 20px;
        border: 2px solid #e0e7ff;
        border-radius: 10px;
        font-size: 1rem;
        transition: all 0.3s;
    }

    .search-input:focus {
        outline: none;
        border-color: #667eea;
        box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
    }

    .search-btn {
        padding: 15px 30px;
        border: none;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: #fff;
        border-radius: 10px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s;
    }

    .search-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
    }

    .pagination {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 20px;
        margin-top: 30px;
        padding: 20px;
    }

    .page-btn {
        padding: 10px 25px;
        border: 2px solid #667eea;
        background: #fff;
        color: #667eea;
        border-radius: 10px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s;
    }

    .page-btn:hover:not(:disabled) {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: #fff;
        transform: translateY(-2px);
    }

    .page-btn:disabled {
        opacity: 0.3;
        cursor: not-allowed;
    }

    .page-info {
        font-weight: 600;
        color: #667eea;
    }

</style>

@code {
    [Parameter]
    public string? StatusFilter { get; set; }
    
    [Parameter]
    public string? Letter { get; set; }
    
    [Parameter]
    public string? SpeciesFilter { get; set; }
    
    [Parameter]
    public string? GenderFilter { get; set; }
    
    [Parameter]
    public string? LocationFilter { get; set; }

    List<Character> characters = new();
    List<Character> filteredCharacters = new();
    List<Character> pagedCharacters = new();
    List<string> availableSpecies = new();
    List<string> availableGenders = new();
    string filterMessage = "Loading characters...";
    string searchText = "";
    int currentPage = 1;
    int pageSize = 20;
    int totalPages = 1;

    protected override async Task OnInitializedAsync()
    {
        if (characters.Any()) return;

        string? url = "https://rickandmortyapi.com/api/character";

        while (!string.IsNullOrEmpty(url))
        {
            try
            {
                var response = await Http.GetFromJsonAsync<CharacterSearchResponse>(url);
                if (response?.Results != null)
                {
                    characters.AddRange(response.Results);
                    url = response.Info?.Next;
                }
                else break;
            }
            catch { break; }
            await Task.Yield();
        }

        ApplyFilter();
    }

    protected override void OnParametersSet()
    {
        if (characters.Any())
        {
            ApplyFilter();
        }
    }

    private void PreviousPage()
    {
        if (currentPage > 1)
        {
            currentPage--;
            UpdatePagedCharacters();
        }
    }

    private void NextPage()
    {
        if (currentPage < totalPages)
        {
            currentPage++;
            UpdatePagedCharacters();
        }
    }

    private void UpdatePagedCharacters()
    {
        totalPages = (int)Math.Ceiling(filteredCharacters.Count / (double)pageSize);
        if (totalPages == 0) totalPages = 1;
        if (currentPage > totalPages) currentPage = totalPages;
        
        pagedCharacters = filteredCharacters
            .Skip((currentPage - 1) * pageSize)
            .Take(pageSize)
            .ToList();
    }

    private void HandleKeyPress(KeyboardEventArgs e)
    {
        if (e.Key == "Enter")
        {
            ApplyFilter();
        }
    }



    private void ApplyFilter()
    {
        var isAllStatus = string.IsNullOrEmpty(StatusFilter) || StatusFilter.Equals("All", StringComparison.OrdinalIgnoreCase) || StatusFilter == "_";
        var hasLetter = !string.IsNullOrEmpty(Letter) && Letter != "_";
        var hasSpecies = !string.IsNullOrEmpty(SpeciesFilter) && SpeciesFilter != "_";
        var hasGender = !string.IsNullOrEmpty(GenderFilter) && GenderFilter != "_";
        var hasSearch = !string.IsNullOrWhiteSpace(searchText);
        
        filteredCharacters = characters
            .Where(c =>
                (isAllStatus || c.Status.Equals(StatusFilter, StringComparison.OrdinalIgnoreCase)) &&
                (!hasLetter || c.Name.StartsWith(Letter, StringComparison.OrdinalIgnoreCase)) &&
                (!hasSpecies || c.Species.Equals(SpeciesFilter, StringComparison.OrdinalIgnoreCase)) &&
                (!hasGender || c.Gender.Equals(GenderFilter, StringComparison.OrdinalIgnoreCase)) &&
                (!hasSearch || c.Name.Contains(searchText, StringComparison.OrdinalIgnoreCase))
            )
            .ToList();

        if (!availableSpecies.Any())
        {
            availableSpecies = characters
                .Select(c => c.Species)
                .Where(s => !string.IsNullOrEmpty(s))
                .Distinct()
                .OrderBy(s => s)
                .ToList();
                
            availableGenders = characters
                .Select(c => c.Gender)
                .Where(g => !string.IsNullOrEmpty(g))
                .Distinct()
                .OrderBy(g => g)
                .ToList();
        }

        filterMessage = $"Found {filteredCharacters.Count} characters";
        currentPage = 1;
        UpdatePagedCharacters();
    }

    private string BuildUrl(string? status, string? letter, string? species, string? gender)
    {
        status = string.IsNullOrEmpty(status) ? "All" : status;
        letter = string.IsNullOrEmpty(letter) ? "_" : letter;
        species = string.IsNullOrEmpty(species) ? "_" : species;
        gender = string.IsNullOrEmpty(gender) ? "_" : gender;
        return $"/Characters/{status}/{letter}/{species}/{gender}";
    }

    public class Character
    {
        [JsonPropertyName("id")] public int Id { get; set; }
        [JsonPropertyName("name")] public string Name { get; set; } = "";
        [JsonPropertyName("status")] public string Status { get; set; } = "";
        [JsonPropertyName("species")] public string Species { get; set; } = "";
        [JsonPropertyName("gender")] public string Gender { get; set; } = "";
        [JsonPropertyName("image")] public string Image { get; set; } = "";
        [JsonPropertyName("episode")] public List<string> Episode { get; set; } = new();
        
        private CharacterLocation? _location;
        [JsonPropertyName("location")] 
        public CharacterLocation? LocationObj 
        { 
            get => _location; 
            set { _location = value; Location = value?.Name ?? ""; } 
        }
        
        [JsonIgnore]
        public string Location { get; set; } = "";
    }
    
    public class CharacterLocation
    {
        [JsonPropertyName("name")] public string Name { get; set; } = "";
    }

    public class CharacterSearchResponse
    {
        [JsonPropertyName("info")] public CharacterInfo Info { get; set; } = new();
        [JsonPropertyName("results")] public List<Character> Results { get; set; } = new();
    }

    public class CharacterInfo
    {
        [JsonPropertyName("next")] public string? Next { get; set; }
    }
}
